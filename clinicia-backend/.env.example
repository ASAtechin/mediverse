# ========================================
# Clinicia Backend - Environment Variables
# ========================================

# Server
PORT=4000
NODE_ENV=development
LOG_LEVEL=debug

# MongoDB (must be a replica set for Change Streams)
DATABASE_URL=mongodb+srv://username:password@cluster.mongodb.net/clinicia?retryWrites=true&w=majority

# Firebase Admin SDK
# Option 1: JSON string of service account
# FIREBASE_SERVICE_ACCOUNT={"type":"service_account","project_id":"..."}
# Option 2: Path to service account JSON file
# GOOGLE_APPLICATION_CREDENTIALS=./service-account.json
# Option 3: Project ID only (limited functionality)
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-project-id

# CORS - comma-separated allowed origins (for production)
ALLOWED_ORIGINS=https://your-web-domain.com,https://your-admin-domain.com
